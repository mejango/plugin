# Exclude everything, then re-include only what the image builds. Bare-name
# patterns here match at ANY depth, so a repo-root exclusion like "lib" would
# also strip web/src/lib. An allow-list has no such ambush.
*
!web
web/node_modules
web/.next
